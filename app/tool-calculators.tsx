"use client";

import Link from "next/link";
import { useState } from "react";

type ToolShellProps = { eyebrow: string; title: string; description: string; children: React.ReactNode };

function ToolShell({ eyebrow, title, description, children }: ToolShellProps) {
  return <main className="min-h-screen bg-zinc-950 px-4 py-8 text-white sm:p-8"><div className="mx-auto max-w-3xl">
    <header className="pt-4 text-center sm:pt-10"><p className="inline-flex rounded-full border border-emerald-400/30 bg-emerald-400/10 px-4 py-1.5 text-xs font-bold uppercase tracking-[0.18em] text-emerald-300">{eyebrow}</p><h1 className="mt-4 text-4xl font-black tracking-tight sm:text-6xl">{title}</h1><p className="mx-auto mt-4 max-w-2xl text-base leading-7 text-zinc-400 sm:text-lg">{description}</p><nav aria-label="CutForecast tools" className="mt-6 flex flex-wrap justify-center gap-x-4 gap-y-2 text-sm font-semibold text-zinc-300"><Link href="/" className="hover:text-emerald-300">Cut calculator</Link><Link href="/body-fat-calculator" className="hover:text-emerald-300">Body fat</Link><Link href="/calorie-deficit-calculator" className="hover:text-emerald-300">Calorie deficit</Link><Link href="/how-long-to-lose-weight" className="hover:text-emerald-300">Timeline</Link></nav></header>
    {children}
    <footer className="mt-10 text-center text-xs text-zinc-500"><Link href="/privacy" className="mr-4 hover:text-zinc-300">Privacy Policy</Link><Link href="/terms" className="hover:text-zinc-300">Terms of Service</Link></footer>
  </div></main>;
}

function NumberField({ label, value, onChange, placeholder, min = 0 }: { label: string; value: string; onChange: (value: string) => void; placeholder: string; min?: number }) {
  return <label className="block text-sm font-medium text-zinc-300">{label}<input type="number" min={min} step="any" value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white placeholder:text-zinc-500 focus:border-emerald-400 focus:outline-none" /></label>;
}

export function BodyFatCalculator() {
  const [sex, setSex] = useState("male"); const [height, setHeight] = useState(""); const [neck, setNeck] = useState(""); const [waist, setWaist] = useState(""); const [hip, setHip] = useState(""); const [weight, setWeight] = useState(""); const [result, setResult] = useState<{ percent: number; fatMass?: number; leanMass?: number } | null>(null); const [error, setError] = useState("");
  const calculate = () => { const h = Number(height) / 2.54; const n = Number(neck) / 2.54; const w = Number(waist) / 2.54; const hips = Number(hip) / 2.54; const circumference = sex === "male" ? w - n : w + hips - n; if (!h || !n || !w || (sex === "female" && !hips) || circumference <= 0) { setError("Enter valid measurements in centimetres. Women also need a hip measurement."); return; } const percent = sex === "male" ? 86.010 * Math.log10(circumference) - 70.041 * Math.log10(h) + 36.76 : 163.205 * Math.log10(circumference) - 97.684 * Math.log10(h) - 78.387; if (!Number.isFinite(percent) || percent < 2 || percent > 70) { setError("Those measurements produced an unrealistic estimate. Check the measurements and try again."); return; } const kg = Number(weight); setResult({ percent: Number(percent.toFixed(1)), fatMass: kg ? Number((kg * percent / 100).toFixed(1)) : undefined, leanMass: kg ? Number((kg * (1 - percent / 100)).toFixed(1)) : undefined }); setError(""); };
  return <ToolShell eyebrow="Circumference estimate" title="Body Fat Calculator" description="Estimate body-fat percentage from height and circumference measurements using the U.S. Navy method."><section className="mt-10 rounded-3xl border border-white/10 bg-zinc-900 p-5 shadow-xl sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-zinc-300">Sex<select value={sex} onChange={(event) => setSex(event.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white"><option value="male">Male</option><option value="female">Female</option></select></label><NumberField label="Height (cm)" value={height} onChange={setHeight} placeholder="e.g. 180" min={120} /><NumberField label="Neck (cm)" value={neck} onChange={setNeck} placeholder="e.g. 38" min={20} /><NumberField label="Waist / abdomen (cm)" value={waist} onChange={setWaist} placeholder="e.g. 84" min={30} />{sex === "female" && <NumberField label="Hips (cm)" value={hip} onChange={setHip} placeholder="e.g. 100" min={40} />}<NumberField label="Weight (kg, optional)" value={weight} onChange={setWeight} placeholder="For fat and lean mass" min={35} /></div><button onClick={calculate} className="mt-6 w-full rounded-xl bg-emerald-400 px-4 py-3 font-bold text-zinc-950 hover:bg-emerald-300">Estimate body fat</button>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-400/30 bg-rose-400/10 p-3 text-sm text-rose-200">{error}</p>}{result && <div className="mt-6 rounded-2xl border border-emerald-400/30 bg-emerald-400/10 p-5 text-center"><p className="text-xs font-bold uppercase tracking-wider text-emerald-300">Estimated body fat</p><p className="mt-1 text-5xl font-black">{result.percent}%</p>{result.fatMass && <div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-zinc-950/60 p-3"><p className="text-zinc-400">Estimated fat mass</p><p className="mt-1 text-xl font-bold">{result.fatMass} kg</p></div><div className="rounded-xl bg-zinc-950/60 p-3"><p className="text-zinc-400">Estimated lean mass</p><p className="mt-1 text-xl font-bold">{result.leanMass} kg</p></div></div>}<p className="mt-4 text-xs leading-5 text-zinc-400">This is a circumference-based estimate, not a medical measurement. Consistent measuring technique matters more than a single number.</p></div>}</section></ToolShell>;
}

export function CalorieDeficitCalculator() {
  const [sex, setSex] = useState("male"); const [age, setAge] = useState(""); const [height, setHeight] = useState(""); const [weight, setWeight] = useState(""); const [activity, setActivity] = useState("1.55"); const [rate, setRate] = useState("0.5"); const [result, setResult] = useState<{ maintenance: number; calories: number; deficit: number } | null>(null); const [error, setError] = useState("");
  const calculate = () => { const a = Number(age); const h = Number(height); const w = Number(weight); if (a < 16 || a > 100 || h < 120 || h > 240 || w < 35 || w > 350) { setError("Enter a realistic age, height, and weight to calculate your target."); return; } const bmr = 10 * w + 6.25 * h - 5 * a + (sex === "male" ? 5 : -161); const maintenance = Math.round(bmr * Number(activity)); const deficit = Math.round(Number(rate) * 7700 / 7); setResult({ maintenance, calories: maintenance - deficit, deficit }); setError(""); };
  return <ToolShell eyebrow="Daily calorie target" title="Calorie Deficit Calculator" description="Estimate maintenance calories and a daily calorie target from your stats, activity, and chosen weekly loss rate."><section className="mt-10 rounded-3xl border border-white/10 bg-zinc-900 p-5 shadow-xl sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-zinc-300">Sex<select value={sex} onChange={(event) => setSex(event.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white"><option value="male">Male</option><option value="female">Female</option></select></label><NumberField label="Age" value={age} onChange={setAge} placeholder="e.g. 30" min={16} /><NumberField label="Height (cm)" value={height} onChange={setHeight} placeholder="e.g. 180" min={120} /><NumberField label="Weight (kg)" value={weight} onChange={setWeight} placeholder="e.g. 82" min={35} /><label className="text-sm font-medium text-zinc-300">Activity level<select value={activity} onChange={(event) => setActivity(event.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white"><option value="1.2">Mostly sedentary</option><option value="1.375">Lightly active</option><option value="1.55">Moderately active</option><option value="1.725">Very active</option></select></label><label className="text-sm font-medium text-zinc-300">Preferred weekly loss<select value={rate} onChange={(event) => setRate(event.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white"><option value="0.25">0.25 kg / week</option><option value="0.5">0.5 kg / week</option><option value="0.75">0.75 kg / week</option></select></label></div><button onClick={calculate} className="mt-6 w-full rounded-xl bg-emerald-400 px-4 py-3 font-bold text-zinc-950 hover:bg-emerald-300">Calculate calorie target</button>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-400/30 bg-rose-400/10 p-3 text-sm text-rose-200">{error}</p>}{result && <div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-zinc-800 p-4"><p className="text-sm text-zinc-400">Estimated maintenance</p><p className="mt-1 text-2xl font-black">{result.maintenance}</p><p className="text-xs text-zinc-500">kcal/day</p></div><div className="rounded-2xl border border-emerald-400/30 bg-emerald-400/10 p-4"><p className="text-sm text-emerald-200">Suggested target</p><p className="mt-1 text-2xl font-black">{result.calories}</p><p className="text-xs text-zinc-400">kcal/day</p></div><div className="rounded-2xl bg-zinc-800 p-4"><p className="text-sm text-zinc-400">Daily deficit</p><p className="mt-1 text-2xl font-black">{result.deficit}</p><p className="text-xs text-zinc-500">kcal/day</p></div></div>}<p className="mt-5 text-xs leading-5 text-zinc-500">This is an estimate. Adjust from weekly average weight trends rather than day-to-day scale changes. If you are pregnant, under 18, managing a medical condition, or have a history of disordered eating, speak to a qualified clinician before restricting calories.</p></section></ToolShell>;
}

export function WeightLossTimelineCalculator() {
  const [start, setStart] = useState(""); const [goal, setGoal] = useState(""); const [rate, setRate] = useState("0.5"); const [weeks, setWeeks] = useState<number | null>(null); const [goalDate, setGoalDate] = useState<string | null>(null); const [error, setError] = useState("");
  const calculate = () => { const s = Number(start); const g = Number(goal); const r = Number(rate); if (s < 35 || g < 35 || g >= s || r <= 0) { setError("Enter a current weight above your goal weight, plus a weekly loss rate."); return; } const estimatedWeeks = Math.ceil((s - g) / r); setWeeks(estimatedWeeks); setGoalDate(new Date(Date.now() + estimatedWeeks * 7 * 24 * 60 * 60 * 1000).toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" })); setError(""); };
  return <ToolShell eyebrow="Goal-date estimate" title="How Long To Lose Weight?" description="Estimate how many weeks a weight-loss goal could take at a pace you choose."><section className="mt-10 rounded-3xl border border-white/10 bg-zinc-900 p-5 shadow-xl sm:p-7"><div className="grid gap-4 sm:grid-cols-3"><NumberField label="Current weight (kg)" value={start} onChange={setStart} placeholder="e.g. 90" min={35} /><NumberField label="Goal weight (kg)" value={goal} onChange={setGoal} placeholder="e.g. 80" min={35} /><label className="text-sm font-medium text-zinc-300">Expected weekly loss<select value={rate} onChange={(event) => setRate(event.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 text-white"><option value="0.25">0.25 kg / week</option><option value="0.5">0.5 kg / week</option><option value="0.75">0.75 kg / week</option><option value="1">1 kg / week</option></select></label></div><button onClick={calculate} className="mt-6 w-full rounded-xl bg-emerald-400 px-4 py-3 font-bold text-zinc-950 hover:bg-emerald-300">Estimate timeline</button>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-400/30 bg-rose-400/10 p-3 text-sm text-rose-200">{error}</p>}{weeks && <div className="mt-6 rounded-2xl border border-emerald-400/30 bg-emerald-400/10 p-6 text-center"><p className="text-xs font-bold uppercase tracking-wider text-emerald-300">Estimated timeline</p><p className="mt-1 text-5xl font-black">{weeks} weeks</p><p className="mt-3 text-zinc-300">Estimated goal date: <strong>{goalDate}</strong></p><p className="mt-4 text-xs leading-5 text-zinc-400">This is a planning estimate. Scale weight naturally varies with water, food intake, and activity, so review progress using a weekly average.</p></div>}</section></ToolShell>;
}
